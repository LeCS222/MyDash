/** Shared limits and patterns used by widgets and backup validation. */
export const ISO_DATE_RE = /^\d{4}-\d{2}-\d{2}$/;
export const WORK_SECONDS = 25 * 60;
export const BREAK_SECONDS = 5 * 60;
export const MAX_WORLD_CLOCK_ZONES = 6;
